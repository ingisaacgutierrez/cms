var express = require('express');
var router = express.Router();
module.exports = router;


/* GET /documents
router.get('/', (req, res) => {
    res.status(200).json({ message: 'GET all documents' });
});

// POST /documents
router.post('/', (req, res) => {
    res.status(201).json({ message: 'POST a new document' });
});

// PUT /documents/:id
router.put('/:id', (req, res) => {
    res.status(200).json({ message: 'PUT update document ' + req.params.id });
});

// DELETE /documents/:id
router.delete('/:id', (req, res) => {
    res.status(200).json({ message: 'DELETE document ' + req.params.id });
});

module.exports = router;
*/