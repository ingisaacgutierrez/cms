var express = require('express');
var router = express.Router();
module.exports = router;


/* GET /messages
router.get('/', (req, res) => {
    res.status(200).json({ message: 'GET all messages' });
});

// POST /messages
router.post('/', (req, res) => {
    res.status(201).json({ message: 'POST a new message' });
});

// PUT /messages/:id
router.put('/:id', (req, res) => {
    res.status(200).json({ message: 'PUT update message ' + req.params.id });
});

// DELETE /messages/:id
router.delete('/:id', (req, res) => {
    res.status(200).json({ message: 'DELETE message ' + req.params.id });
});

module.exports = router;*/
